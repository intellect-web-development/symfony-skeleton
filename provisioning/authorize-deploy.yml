---
-   name: Put deploy authorized key
    hosts: all
    remote_user: root
    gather_facts: no
    tasks:
        -   name: Add user authorized key
            authorized_key:
                user: deploy
                key: "{{ lookup('file', 'files/deploy_rsa.pub') }}"
